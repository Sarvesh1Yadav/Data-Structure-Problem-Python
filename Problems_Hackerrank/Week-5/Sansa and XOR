Problem Statement - Sansa has an array. 
She wants to find the value obtained by XOR-ing the contiguous subarrays, followed by XOR-ing the values thus obtained. Determine this value.


import math
import os
import random
import re
import sys

def sansaXor(arr):
    # Write your code here
    
    n = len(arr)
    p  = arr[0]
    if n%2==0:
        return 0
    else:
        for i in range(2,n,2):
            p = p^arr[i]
        return p


if __name__ == '__main__':
    fptr = open(os.environ['OUTPUT_PATH'], 'w')

    t = int(input().strip())

    for t_itr in range(t):
        n = int(input().strip())

        arr = list(map(int, input().rstrip().split()))

        result = sansaXor(arr)

        fptr.write(str(result) + '\n')

    fptr.close()
