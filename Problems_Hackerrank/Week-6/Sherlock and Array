Problem stament - Watson gives Sherlock an array of integers.
His challenge is to find an element of the array such that the sum of all elements to the left is equal to the sum of all elements to the right.


import math
import os
import random
import re
import sys

def balancedSums(arr):
    # Write your code here
    total_sum = sum(arr)
    left_sum = 0
    for i in arr:
        right_sum = total_sum-left_sum-i
        if left_sum==right_sum:
            return 'YES'
        left_sum+=i
    return 'NO'

if __name__ == '__main__':
    fptr = open(os.environ['OUTPUT_PATH'], 'w')

    T = int(input().strip())

    for T_itr in range(T):
        n = int(input().strip())

        arr = list(map(int, input().rstrip().split()))

        result = balancedSums(arr)

        fptr.write(result + '\n')

    fptr.close()
