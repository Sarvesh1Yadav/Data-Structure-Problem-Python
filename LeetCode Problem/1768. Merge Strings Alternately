class Solution:
    def mergeAlternately(self, word1: str, word2: str) -> str:
        l=''
        n1 = len(word1)
        n2 = len(word2)
        for i in range(n1+n2):
            if i<n1:
                l+=word1[i]
            if i<n2:
             l+=word2[i]
        return l
