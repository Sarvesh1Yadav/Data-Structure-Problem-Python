import math
class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        n=len(nums)
        f = math.floor(n/2)
        freq = {}
        for num in nums:
            if num in freq:
                freq[num]+=1
            else:
                freq[num]=1
        for key,value in freq.items():
            if value>f:
                return key
        
