def insertion(arr):
        for i in range(len(arr)):
            for j in range(i,0,-1):
                if arr[j-1]>arr[j]:
                    arr[j-1],arr[j] = arr[j],arr[j-1]
        return arr
arr = [ 4,5,2,6,1,9,23,21]
print(insertion(arr))
