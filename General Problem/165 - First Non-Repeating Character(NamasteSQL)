def first_non_repeating_char(s: str) -> str:
    if len(s) == 0:
        return None

    n = len(s)

    for i in range(n):
        count = 0
        for j in range(n):
            if s[i] == s[j]:
                count += 1
        if count == 1:
            return s[i]

    return None
