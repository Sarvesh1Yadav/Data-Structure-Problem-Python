Problem Statement - Given two strings, s1 and s2, determine whether s2 is an anagram of s1.
An anagram is created by rearranging the letters of one string to form another string, using every letter from the original exactly once without adding or omitting any characters.

class solution:
    def validAnagram(self, s1, s2):
        #Write your code here...
        if len(s1)!=len(s2):
            return False
        freq1={}
        x=False
        for num1 in s1:
            if num1 in freq1:
                freq1[num1]+=1
            else:
                freq1[num1]=1
        freq2={}
        for num2 in s2:
            if num2 in freq2:
                freq2[num2]+=1
            else:
                freq2[num2]=1
        if freq2==freq1:
            return True
        else:
            return False
                    
